#include <bits/stdc++.h>
using namespace std;

int main(){
    // Find inorder of BST and store it in the vector 
    // As inorder is in sorted form
    // So exchange the data of BST by inorder vector.

    // Not , BST has changed into  min HEAP!!
}